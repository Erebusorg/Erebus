[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.28"
optimizer = true
optimizer_runs = 200
# The spend tests replay a proof produced by `mixnet/crates/fees`.
fs_permissions = [{ access = "read", path = "./test/fixtures" }]

[fmt]
line_length = 100
